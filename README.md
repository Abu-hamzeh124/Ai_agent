AI Code Assistant هو مساعد برمجي يعتمد على Google Gemini 2.0، يمكنه الإجابة عن أسئلة برمجية وتنفيذ استدعاءات دوال مخصصة مع الحفاظ على سياق المحادثة.

🚀 المميزات

إجابات برمجية دقيقة باستخدام نموذج Gemini 2.0.

دعم Function Calls: استدعاء دوال خارجية ومعالجة الردود تلقائيًا.

حفظ سياق المحادثة بين الاستدعاءات المختلفة.

إمكانية تشغيل البرنامج من CLI مع خيار --verbose لمزيد من التفاصيل.

⚙️ المتطلبات

Python 3.10 أو أحدث

مكتبات Python:

pip install google-genai python-dotenv


ملف .env يحتوي على مفتاح API:

API_KEY=YOUR_GOOGLE_GENAI_API_KEY

🏁 طريقة التشغيل
python main.py "your prompt here" [--verbose]

أمثلة:
python main.py "Explain how the calculator renders the result to the console"
python main.py "Show me a Python function to sort a list" --verbose


الخيار --verbose يعرض معلومات إضافية عن Prompt Tokens، Response Tokens، والردود التفصيلية.

📂 بنية المشروع
.
├── main.py               # الملف الرئيسي لتشغيل المساعد
├── call_function.py      # دوال مساعدة لاستدعاءات Functions
├── prompts.py            # prompts للنموذج
├── .env                  # مفتاح API الخاص بـ Google GenAI
├── README.md             # هذا الملف

⚠️ ملاحظات

جميع الردود من الدوال يتم تحويلها تلقائيًا إلى نصوص قبل إضافتها إلى المحادثة.

تأكد من أن ردود الدوال نصية أو قابلة للتحويل إلى نص.

البرنامج مصمم للعمل عبر CLI فقط ولا يتضمن واجهة رسومية.

💡 نصائح

استخدم --verbose أثناء التطوير لمعرفة تفاصيل عمل النموذج والدوال.

يمكن تعديل prompts.py لإضافة System Prompts مخصصة حسب احتياجك.

يمكنك إضافة دوال جديدة إلى call_function.py لاستدعاء إجراءات مخصصة.
